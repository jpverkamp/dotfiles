#!/usr/bin/env python3

import sys
import time

for line in sys.stdin:
    now = time.strftime("[%Y-%m-%d %H:%M:%S]", time.localtime())
    sys.stdout.write('{0} {1}'.format(now, line))
    sys.stdout.flush()
